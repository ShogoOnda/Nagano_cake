<div class="container my-5">
  <div class="row">
    <div class="col-md-auto mb-3 bg-light">
      <h4>注文履歴一覧</h4>
    </div>
  </div>

  <table class="table table-hover table-inverse">
    <thead>
      <tr>
        <th>購入日時</th>
        <th>購入者</th>
        <th>注文個数</th>
        <th>注文ステータス</th>
      </tr>
    </thead>
    <% @orders.each do |order| %>
      <tbody>
        <tr>
            <td><%=link_to order.created_atstrftime("%Y年%m月%d日"), admin_order_path(order.id) %></td>
            <td><%=order.name %></td>
            <% total == 0 %>
            <% order.order_details.each do |order_detail| %>
              <%= total += order_details.amount %>
            <% end %>
            <td><%= total %></td>
            <td><%= order.status_i18n %></td>
        </tr>
      </tbody>
    <% end %>
  </table>
</div>